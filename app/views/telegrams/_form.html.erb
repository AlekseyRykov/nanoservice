<%= form_with(model: telegram, local: true) do |form| %>
  <% if telegram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(telegram.errors.count, "error") %> prohibited this telegram from being saved:</h2>

      <ul>
      <% telegram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.text_field :article_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
