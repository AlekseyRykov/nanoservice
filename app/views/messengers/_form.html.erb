<%= form_with(model: messenger, local: true) do |form| %>
  <% if messenger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(messenger.errors.count, "error") %> prohibited this messenger from being saved:</h2>

      <ul>
      <% messenger.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
